#light url shortener
because all the shorteners are way too annoying to deal with for when you want to be the only one shortening urls.

compile with `cargo build --release`
run with `HOST` `PORT` `BASE_URL` and `ADD_TOKEN` enviroment variables, then reverse proxy.